import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { bannerStyles } from './Styles/style.banner'

const Banner = () => {
  return (
    <View style={bannerStyles.wrapper}>
      <Text>Banner</Text>
    </View>
  )
}

export default Banner

const styles = StyleSheet.create({})